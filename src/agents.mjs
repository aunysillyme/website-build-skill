// Planned native generation. Unknown host schemas must fail closed.
// Required future implementation: full canonical bodies, reference rebasing,
// observed tool mapping, read-only Reviewer, research-first setup acknowledgements,
// Hermes declarative/session branch evidence and separate activation receipts.
export function generateAgents(_request) {
  throw Error('UNAVAILABLE: native agent generation is planned; use the full team bundle.');
}
